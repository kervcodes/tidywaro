# Tidywaro — Claude Code entry point

@AGENTS.md

Use the shared repository instructions above so Codex and Claude follow the same product scope, stack constraints, design direction, and verification rules. Read only the task-relevant documents linked there.

For Claude chat/design tools without local repository access, attach the relevant Markdown files and approved frames explicitly; do not assume this import gives a browser chat access to the clone.
